[sshd]
enabled       = true
backend       = polling
journalmatch  =
maxretry      = 5
filter        = sshd
logpath       = /var/log/auth.log
action        = nftables-multiport[name="%(__name__)s", port="22,8022", protocol="tcp"]
